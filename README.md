windows-offline-patching
========================

Script to patch wim-files

- uses dism for mounting and adding packages
- needs a wds (path must be changed in the script)
- can update win7, win8, win8.1